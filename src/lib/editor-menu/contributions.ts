// Eagerly imported by the editor host so every feature module's
// `registerEditorMenuItem(...)` runs before the context menu renders. Each feature
// adds ONE import line here for its contribution module. Append-only, like
// `src/components/panels/index.ts`.
//
// e.g.
//   import "@/lib/lookup/editor-menu"; // Wave 2 Stream B — Look up / Find another word

export {};
